// Не используется, поскольку вся платформозависимая логика вынесена в класс АЦП контроллера
#pragma once

#include "oscil_virtual.h" // Общий для осцилографов класс
#include "oscil.h"         // Одиночное считывание (Single)
#include "oscil_logic.h" // Множественное считывание (Continue)